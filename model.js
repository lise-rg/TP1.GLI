// Implémenter ici les 4 classes du modèle.
// N'oubliez pas l'héritage !

/**
Une forme possède une couleur et une épaisseur de trait. Un rectangle possède des coordonnées de
son point haut-gauche, une largeur et une hauteur. Une ligne possède les coordonnées de ses deux points.
**/

function Drawing() {
    var shapes[];
}

function Shape(color, lineWidth) {

    function Line(color, lineWidth, startX, startY, endX, endY) {
      //Shape.call(this, color, lineWidth);
    };

    function Rectangle(color, lineWidth) {
      //Shape.call(this, color, lineWidth);
    };

}
